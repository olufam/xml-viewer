<?xml version="1.0" encoding="utf-8"?>
<t24help>
<header>
<product>DS</product>
<table>DS.PARAMETER</table>
</header>
<overview>
<ovdesc>

        <p>Parameter table for Design Studio Installer. </p>
        <p>Specifies the version, which can be used in OFS message, to create respective file records and order to sort the OFS messages.</p>
    </ovdesc>
<ovimage></ovimage>
</overview>
<menu>
<t>
<field>VERSION.NAME</field>
<desc>
        <p>Specifies the application and version, by which the records are created via OFS.</p>
        <p>If <b>DS.PARAM.ID</b> = <b>VERSION</b>, then <b>VERSION.NAME</b> can have any existing version id of the application called <b>'VERSION'</b>. </p>
        <p>If <b>DS.PARAM.ID</b> = <b>ENQUIRY</b>, then <b>VERSION.NAME</b> can have any existing enquiry id of the application called <b>'ENQUIRY'</b>.</p>
        <p>For e.g.</p>
        <p>To create a record in <b>VERSION</b> application, the value is specified as <b>VERSION,DS</b> which will be used in OFS message.</p>
        <p>The 'DS' is the pgm version for other files, which are specified in <b>FILE.SORTING</b> field.</p>
        <p>If the <b>FILE.SORTING</b> field is specified with the files <b>EB.API</b>, <b>PGM.FILE</b> and <b>EB.DICTIONARY</b> then the version installer will use the versions such as <b>'EB.API,DS'</b> , <b>'PGM.FILE,DS'</b> and <b>'EB.DICTIONARY,DS'</b> respectively to form OFS messages.</p>
        <p>Validation Rules: </p>
        <p>
            <p value="1">If <b>DS.PARAM.ID</b> = <b>VERSION</b> (installer type), then the application name in <b>VERSION.NAME</b> field should be <b>VERSION</b>. </p>
            <p value="2">If <b>DS.PARAM.ID</b> = <b>ENQUIRY</b> (installer type), then the application name in <b>VERSION.NAME</b> field should be <b>ENQUIRY</b>.</p>
            <p> This field cannot be blank if valid application name given as @ID </p>
            <p value="3">Must be a valid entry in <b>VERSION</b> table.</p>
            <p value="5">Allows maximum of 35 alphanumerics.</p>
        </p>
    </desc>
</t>
<t>
<field>DESCRIPTION</field>
<desc>
        <p>Specifies the description of DS parameter of respective installer. </p>
        <p>Validation Rules: </p>
        <p>
            <p value="1">Allows maximum of 35 alphanumerics.</p>
            <p> Mandatory field</p>
        </p>
    </desc>
</t>
<t>
<field>FILE.SORTING</field>
<desc>
        <p>Specifies the order, in which the OFS messages are sorted, before pumping it into T24 system.</p>
        <p>Before creating the main file record, the dependent file records are created.</p>
        <p>In case of <b>VERSION</b> deployment from design studio, the received xml can have dependent file records. The <b>EB.API</b>, <b>PGM.FILE</b> and <b>EB.DICTIONARY</b> are dependent files for <b>VERSION</b>. </p>
        <p>Hence the dependent file records are created first and then <b>VERSION</b> record is created. </p>
        <p>For this purpose, the sorting order of OFS messages must be specified in this field.</p>
        <p>Validation Rules: </p>
        <p>
            <p value="1">Must be a valid application name. </p>
            <p value="2">The dependent files should be specified before the <span style="background-color: #ffffff;">main</span>file.</p>
        </p>
    </desc>
</t>
<t>
<field>PARAM.ID</field>
<desc>
        <p>Unique reference to indentify parameter table for different type of installers.</p>
        <p>Validation Rules:</p>
        <p>
            <p value="1">Allowed values are: SYSTEM, valid application names like <b>VERSION</b>, <b>ENQUIRY</b>, <b>LOCAL.TABLE</b> (Installer types applicable for respective applications) </p>
            <p value="2">Allows maximum of 20 characters.</p>
        </p>
    </desc>
</t>
<t>
<field>LOCAL.LIB.DIR</field>
<desc>
        <p>Defines the directory used to store libraries which comes with packager which is created via  Design studio</p>
        <p>Directory given in this field will get created under run directory during DS.PARAMETER table authorization </p>
        <p>Accepts valid path of existing directory </p>
        <p>Validation Rules:</p>
        <p>
            <p value="1">Accepts input only if the PARAM.ID is SYSTEM</p>
            <p value="1"> Must be a type 1 or 19 (UD type) file</p>
            <p value="1"> Becomes noinput when VERSION.NAME and FILE.SORTING fields given</p>
            <p value="2">Allows maximum of 50 characters.</p>
        </p>
    </desc>
</t>
<t>
<field>LOCAL.BIN.DIR</field>
<desc>
        <p> Defines the directory used to store binaries which comes with packager which is created via Design studio</p>
        <p>Directory given in this field will get created under run directory during DS.PARAMETER table authorization </p>
        <p>Accepts valid path of existing directory</p>
        <p>Validation Rules:</p>
        <p>
            <p value="1">Accepts input only if the PARAM.ID is SYSTEM</p>
            <p value="1"> Must be a type 1 or 19 (UD type) file</p>
            <p value="1"> Becomes noinput when VERSION.NAME and FILE.SORTING fields given</p>
            <p value="2">Allows maximum of 50 characters.</p>
        </p>
    </desc>
</t>
<t>
<field>LOCAL.INSERT.DIR</field>
<desc>
        <p> Defines the directory used to store inserts which comes with packager which is created via Design studio</p>
        <p>Directory given in this field will get created under run directory during DS.PARAMETER table authorization </p>
        <p>Accepts valid path of existing directory</p>
        <p>Validation Rules:</p>
        <p>
            <p value="1">Accepts input only if the PARAM.ID is SYSTEM</p>
            <p value="1"> Must be a type 1 or 19 (UD type) file</p>
            <p value="1"> Becomes noinput when VERSION.NAME and FILE.SORTING fields given</p>
            <p value="2">Allows maximum of 50 characters.</p>
        </p>
    </desc>
</t>
</menu>
</t24help>

