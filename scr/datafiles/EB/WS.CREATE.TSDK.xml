<?xml version="1.0" encoding="utf-8"?>
<t24help>
<header>
<product>EB</product>
<table>WS.CREATE.TSDK</table>
</header>
<overview>
<ovdesc>

            <p>Creates the T24 Software Development Toolkit (TSDK) Java Archive file.</p>
            <p>When a record is Verified T24 dynamically creates a Java file for each T24 application.</p>
            <p>The Java file is then compiled and added to the Java Archive file (JAR).</p>
            <p>This can then be distributed to T24 Java developers for use with the TSDK Foundation classes.</p>
        </ovdesc>
<ovimage></ovimage>
</overview>
<menu>
<t>
<field>WS.CREATE.ID</field>
<desc>
            <p>The Id of this record.</p>
        </desc>
</t>
<t>
<field>DEPLOY.TYPE</field>
<desc>
            <p>The key to the WS.DEPLOY file containing details of the deployment descriptor for the TSDK.</p>
        </desc>
</t>
<t>
<field>JAVA.HOME</field>
<desc>
            <p>The directory where Java has been installed (e.g. /usr/java131).</p>
        </desc>
</t>
<t>
<field>OUTPUT.PATH</field>
<desc>
            <p>The base directory where the TSDK will be created (e.g. tsdk).  When the WS.CREATE.TSDK record is Verified "src" and "classes" directories will be created in the output path directory containing the Java source and class files.  The class files will then added to a JAR file.</p>
        </desc>
</t>
</menu>
</t24help>

